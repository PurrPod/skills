#!/usr/bin/env python3
"""
paper-reading-mentor 阶段管理器
用法: python3 scripts/stage_manager.py --stage=<1|2|3|4|5> --workdir=<工作目录>

--stage:   阶段编号 1-4
--workdir: 工作目录（存放各阶段 markdown 文件的位置），默认为当前目录

注意：该脚本的 prompts 采用自问自答式设计——每个 prompt 中嵌入选择题，
要求 agent 在文档中自行提问、自行解答，行云流水，一气呵成。

所有 prompts 均保持论文无关（paper-agnostic），适配任意领域论文。
"""

import argparse
import os
import sys


def check_prereq(stage: int, workdir: str):
    """检查前置 stage 文件是否存在"""
    names = {1: "PreKnowledgeTree.md", 2: "PaperRebirth.md",
             3: "ArchitectureBlueprint.md", 4: "ExecutionCompanion.md",
             5: "ResearchEvolution.md"}
    for s in range(1, stage):
        fname = os.path.join(workdir, names[s])
        if not os.path.isfile(fname):
            print(f"❌ 前置任务未完成：在 {workdir} 下未找到 {names[s]}")
            print(f"请先完成第 {s} 阶段后再继续。")
            sys.exit(1)


PROMPTS = {
    1: """# Stage 1 任务：前置知识清单

你的任务是根据论文内容，整理一份前置知识清单。

## 指令

识别阅读这篇论文需要的前置知识。按依赖关系从基础到专用组织成知识树。

**对每个知识模块，你需要写三件事：**
1. 这个知识是什么（1-2段）
2. **为什么这篇论文需要它**——用"因为论文里会用到..."的句式建立关联
3. 举一个具体的例子（数字、向量、伪代码均可）

**知识组织的通用结构：**
- 基础层：最底层的学科基础概念
- 中间层：论文所用方法论的基础
- 专用层：这篇论文最特定的假设/技术

**格式要求：**
- 短句分行，大量空行制造节奏
- 自问自答：在文档里问"为什么要知道这个？"然后自己回答
- 末尾加一行检查清单："如果你掌握了以上 N 个知识点，就可以进入 Stage 2"

**不要**写成格式化的列表。写得像一个老师在黑板上逐步推导。
""",

    2: """# Stage 2 任务：发明者思维复盘

你已经完成了 Stage 1，构建了前置知识。现在你的任务是：

**以"论文作者"的第一视角，重新走一遍这篇论文的诞生过程。**

## 指令

这篇文档是**自问自答**的。你要模拟一个场景：

> 你是作者。回到论文发表前的时间点。你看到了一些别人没注意到的现象。
> 现在你要一步步决定下一步做什么。

**文档结构必须是决策链风格。在每个关键决策点：**

1. **设景**："假设你是作者，现在你看到了..."
2. **提问**："如果你遇到这种情况，你会怎么选？A/B/C"
3. **自答**："作者实际选了 X，因为..."
4. **解释**：从直觉推到数学/技术细节

**典型决策链模板（根据实际论文调整结构）：**

```
观察到现有方法的局限
  → 问：该从哪个角度改进？
    → 选方向 → 遇到技术难点 → 怎么绕过？
      → 方法成型 → 问：如何验证/扩展？
        → 实验设计 → 给出理论保证/边界条件
```

**格式要求：**
- 短句分行，大量空行
- 用 ASCII 箭头（↓ →）展示流程
- 用具体数字/数值举例（不要只写抽象概念）
- 论文公式出现前先解释直觉
- 末尾给出完整的决策链总结

**不要**像念论文摘要一样写。要让读者觉得自己在亲手做研究。
""",

    3: """# Stage 3 任务：代码架构重建

你已经用发明者视角复盘了论文。现在你的任务是：

**不读源码，从第一性原理推导出代码应该长什么样。**

## 指令

这篇文档遵循"架构优先"原则，从数据流图推导出代码结构。

**写作顺序（严格按这个顺序，不要跳步）：**

### 1. 系统建模
画出数据流图。这个系统里有哪些对象？真正流动的数据是什么？
```
[输入] → [处理单元1] → [核心数据] → [处理单元2] → [输出]
```
标注每个环节对应的论文概念。

### 2. Repository 设计
从数据流推导文件结构。每个文件对应一个职责。
解释"为什么是这 N 个文件"——一个文件不多，一个文件不少。

### 3. Skeleton 先跑
写出最简运行循环（不包含论文创新点），证明基础系统就能工作。

### 4. 创新点从哪里切入
重新审视数据流。论文的核心贡献应该插在哪两个环节之间？为什么？

### 5. 公式即设计语言
- 用自然语言描述设计目标（一句话）
- 逐步推导出数学约束
- 每个约束 → 一个公式 → 2-4 行符合编程风格的伪代码（不要使用注释型伪代码，行数可根据实际情况调整）

### 6. 结尾映射表
| 作者想法 | 数学表达 | 代码实现 | 软件职责 |
|---|---|---|---|

**格式要求：**
- 用 ``` 画流程图和代码块
- 用 $ 包裹数学公式
- 让读者感觉"我甚至不用看源码就能猜出 repo 结构"

**注意**：不要直接从论文里抄公式。先给设计约束，再自然导出公式。
""",

    4: """# Stage 4 任务：让论文活起来

你已经完成了架构推导。现在你的任务是：

**带读者亲眼看到论文的公式在代码运行中真实发生。**

## 指令

如果论文有开源代码，阅读真实源码后写出这份文档。
如果没有开源代码，用 Stage 3 推导出的伪代码来演示。

**核心策略：分层递进。先跑起来，再谈复现。**

不要一上来就试图复现论文的完整实验（72h 训练 / 8×A100 等）。
很多论文的官方实验你根本跑不了。

按以下优先级递进：

### Tier 1 — CPU 可运行的最小 Demo（最优先）
- 提取论文核心机制中**不需要 GPU / 不需要长训练**的部分
- 写一个能在普通笔记本 CPU 上几秒内跑完的脚本
- 例如：单步前向传播、一次梯度更新、一个小规模 toy example
- 目的是：让读者亲眼看到核心公式在 CPU 上执行

### Tier 2 — 缩小版 Demo
- 在 Tier 1 基础上，增加参数规模、运行轮次，但仍限定在可执行范围内
- 展示关键指标的变化趋势（如 loss 下降、准确率上升）
- 如果论文有公开的 pre-trained model / checkpoint，尝试加载并跑推理

### Tier 3 — 介绍官方实验（注明不可复现）
- 描述论文的官方实验配置（数据集、超参、硬件）
- 如果因为资源限制无法复现，**明确标注**：以下结果来自论文原文，非本机运行
- 展示论文的 Figure / Table，并解释"如果跑通了应该看到什么"

**文档结构（五段式）：**

### 1. 运行前——先解释即将看到什么
- 每个实验配置对应论文的哪个设定
- 预期输出/指标对应论文的哪张图/表

### 2. 运行中——Live Paper Mode
追踪关键代码路径：
- "当这行代码执行时，论文 Eq.(X) / Algorithm Y 正在运行"
- 高亮那 1-2 行最关键的实现细节（论文的核心技巧）
- "指标从这里开始变化——对应论文 Figure X"

### 3. 运行后——Experiment Autopsy
- "为什么指标最终稳定在这个值？"
- "如果改变这个超参数会怎样？"
- "这个现象对应论文的 Theorem X / Lemma X"

### 4. Verification Checklist（验证清单）

这不是结束。这是给未来读者的复现手册。

列出验证论文可复现性的**关键检查节点**：

- 每个检查节点：**预期输出是什么？**
- **如果没达到预期，可能是什么原因？**
- 常见的错误标志和调试建议
- 例如：运行某段代码后应看到 xxx 数值，如果看到 yyy 则说明环境有问题

让读者拿到这段代码后，能自己判断"哪里没成功"。

### 5. 末尾总结表
| 实验 | 结果 | 论文图表 | 关键代码 |
|---|---|---|---|

**格式要求：**
- 代码行和公式要交叉引用
- 用具体数字（不要只说"提升/下降"，要给出具体值）
- 结尾写："这篇论文不再是一堆静态文字——你刚刚亲眼看着它执行了一遍。"

**如果无法访问代码仓库**，在文件开头用显眼免责声明标明。
""",

    5: """# Stage 5 任务：研究进化（ResearchEvolution）

你已经完成了论文的完整拆解。现在你的任务是：

**以 Reviewer 的身份，对这篇论文进行一次证据导向的批判性分析。**

## 指令

这篇文档是你作为审稿人写的审稿意见。不是表扬信。

**核心原则：**
- 观点要**具体**。观点要有**证据**。
- 避免泛泛而谈。
- 指出证据不足、实验缺失、假设限制和潜在替代解释。

**文档结构（六大板块，必须全部覆盖）：**

### 1. Reject 的理由
如果我是审稿人，我会因为什么理由拒掉这篇论文？
- 方法上有无漏洞？假设是否合理？
- 实验设计有无缺陷？基线选择是否公平？
- 结论是否被实验结果充分支持？
- 写作上有无重大问题？

每个理由都要**具体、尖锐**——不是"实验不够多"，而是"Figure 3 只跑了 3 个 seed，方差没展示"。

### 2. 缺失的实验
- 哪些关键实验没有做？
- 如果补上这些实验，结果可能会翻转吗？
- 有没有遗漏的消融实验（ablation study）？

### 3. 不严谨之处
- 数学推导中有无跳步/隐含假设？
- 边界条件是否被忽略了？
- 有没有 cherry-pick 结果的嫌疑？

### 4. 未来改进方向
- 如果作者要 rebuttal，可以从哪些角度回应？
- 哪些地方修一修还能发？

### 5. 创新点挖掘——如何发下一篇论文
- 基于这篇论文，还能做什么新东西？
- 故事怎么讲？切入点是什么？
- 能不能换个领域、换个设定、换个损失函数做成新工作？
- 投入产出比如何？（容易做但有价值 vs 难做但突破性大）

### 6. 领域空白与方向判断
- 这个子领域还有哪些明确的空白？
- 哪些方向值得投入，哪些是坑？
- 未来 1-2 年最有希望的 research direction 是什么？

**格式要求：**
- 每条意见要有编号，方便引用
- 用尖锐但专业的语气——像真的在写审稿意见
- 结尾给一个整体评分（Accept / Weak Accept / Weak Reject / Reject）
- 如果是 Reject，给一个具体的 rebuttal 路径
"""
}


def main():
    parser = argparse.ArgumentParser(description="paper-reading-mentor 阶段管理器")
    parser.add_argument("--stage", type=int, required=True, choices=[1, 2, 3, 4, 5],
                        help="阶段编号 1-5（5 为可选的 Reviewer 研究进化分析）")
    parser.add_argument("--workdir", type=str, default=".",
                        help="工作目录（存放 stage 文件的位置），默认当前目录")
    args = parser.parse_args()

    workdir = args.workdir
    stage = args.stage

    # 前置检查
    if stage >= 2:
        check_prereq(stage, workdir)

    # 输出对应阶段的 prompt
    print(PROMPTS[stage].strip())


if __name__ == "__main__":
    main()
